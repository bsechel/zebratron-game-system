{
  "name": "zebratron-game-system",
  "version": "0.1.0",
  "description": "A modern 8-bit game system for web browsers",
  "main": "runtime/dist/index.js",
  "type": "module",
  "scripts": {
    "build:wasm": "wasm-pack build core --target web --out-dir ../runtime/pkg",
    "build": "npm run build:wasm && cd runtime && npm run build",
    "dev": "cd runtime && npm run dev",
    "test": "cargo test && cd runtime && npm test",
    "lint": "cargo clippy --all-targets --all-features && cd runtime && npm run lint",
    "typecheck": "cd runtime && npm run typecheck"
  },
  "workspaces": [
    "runtime",
    "tools/*"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/yourusername/ZebratronGameSystem.git"
  },
  "keywords": [
    "game-system",
    "8-bit",
    "retro",
    "webassembly",
    "game-engine"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "wasm-pack": "^0.12.1"
  }
}